# Boilerplate-Crud-Api

This project was created as a template for a POSTGRES CRUD. It can be used to start a project, essentially serving as a `boilerplate`

# Next Steps

## 1. Install dependencies

```bash
npm install
```

```bash
pnpm
```

```bash
yarn
```

## 2. Configure environment variables

```ts
DATABASE_URL =
  'postgresql://default-db_owner:PEIOXVGJpg62@ep-fragrant-river-a5xbbd1f.us-east-2.aws.neon.tech/default-db?sslmode=require';
```

Note: This example uses the Neon Console. They provide the URL ready to use. Obviously, this example database has already been deleted.

# Well, is done 🚀 | ;)
